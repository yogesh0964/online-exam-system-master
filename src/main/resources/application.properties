spring.application.name=online-exam
server.port=${PORT:8080}

# ? Database ? Railway env variables + localhost fallback
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://localhost:3306/examess_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${JDBC_DATABASE_USERNAME:root}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:yogi@0964}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# H2 off
spring.h2.console.enabled=false

# File upload fallback
file.upload-dir=./uploads/

# JWT
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=36000000

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dienfck3a}
cloudinary.api-key=${CLOUDINARY_API_KEY:575985832241197}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:YOUR_API_SECRET_YAHAN_DAALO}